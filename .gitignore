# Python
__pycache__/
*.pyc

# Logs (se regeneran solos, no hace falta versionarlos)
logs/

# Artefactos de exploracion/debug locales, no los usa la app
_explore.txt
_pexplore.txt
_names_us.txt
_scrape_jug.log
_understat_raw.pkl

# Config local del editor/preview (especifico de esta maquina)
.claude/

# NOTA: cache/ y modelo_config.json y alias_equipos.json se commitean
# a proposito (ver instrucciones de despliegue): sin cache/ la app
# depende de descargar todo en vivo la primera vez que alguien la abre
# en la nube, lo cual es lento y menos confiable.

# Temporales de escritura atomica (cache/*.csv.tmp) si un proceso se corta
*.tmp
