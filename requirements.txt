# Dependencias para desplegar app.py (Streamlit Community Cloud, Docker
# o cualquier host con Python >= 3.11 -- pandas 3 no soporta menos).
# Versiones ancladas a las que se probaron end-to-end en este proyecto
# (predicciones, backtest, descarga real de datos) antes de desplegar.
#
# streamlit y pandas son obligatorios: sin ellos app.py no arranca.
streamlit==1.64.0
pandas==3.0.5

# Descarga de xG desde Understat (scraping). Anclado a proposito:
# red_understat.py se engancha a internos de soccerdata (_request_api,
# _init_session) para reintentar los errores "TLS Client Error". Antes de
# subir esta version, correr `python xg_loader.py --refrescar` y revisar
# que no aparezca el aviso "Understat queda sin blindar" en los logs.
#
# Es OPCIONAL en tiempo de ejecucion: xg_loader.py solo lo importa dentro
# de las funciones que descargan, asi que con el cache/ commiteado la app
# predice sin el. Solo hace falta para "Actualizar datos" y para el
# actualizador en segundo plano (actualizador.py).
soccerdata==1.9.1

# Cliente HTTP alternativo de red_understat.py cuando el cliente TLS de
# soccerdata falla (ya lo trae streamlit; se declara porque se usa directo).
requests>=2.32
